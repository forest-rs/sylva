[workspace]
resolver = "2"
members = [
    "crates/sylva_grow",
    "crates/sylva_mesh",
    "crates/sylva_skeleton",
    "crates/sylva_species",
    "examples/skeleton_dump",
    "examples/species_gallery",
]

[workspace.package]
edition = "2024"
# Keep in sync with RUST_MIN_VER in .github/workflows/ci.yml and with the
# MSRV noted in README.md.
rust-version = "1.92"
license = "Apache-2.0 OR MIT"
repository = "https://github.com/forest-rs/sylva"

[workspace.lints]
# LINEBENDER LINT SET - Cargo.toml - v8
# See https://linebender.org/wiki/canonical-lints/
rust.keyword_idents_2024 = "forbid"
rust.non_ascii_idents = "forbid"
rust.non_local_definitions = "forbid"
rust.unsafe_op_in_unsafe_fn = "forbid"

rust.elided_lifetimes_in_paths = "warn"
rust.missing_debug_implementations = "warn"
rust.missing_docs = "warn"
rust.trivial_numeric_casts = "warn"
rust.unnameable_types = "warn"
rust.unreachable_pub = "warn"
rust.unused_import_braces = "warn"
rust.unused_lifetimes = "warn"
rust.unused_macro_rules = "warn"
rust.unused_qualifications = "warn"

rust.unsafe_code = "deny"

clippy.too_many_arguments = "allow"

clippy.allow_attributes_without_reason = "warn"
clippy.cast_possible_truncation = "warn"
clippy.cast_possible_wrap = "warn"
clippy.collection_is_never_read = "warn"
clippy.dbg_macro = "warn"
clippy.debug_assert_with_mut_call = "warn"
clippy.default_trait_access = "warn"
clippy.doc_markdown = "warn"
clippy.fn_to_numeric_cast_any = "warn"
clippy.infinite_loop = "warn"
clippy.large_stack_arrays = "warn"
clippy.mismatching_type_param_order = "warn"
clippy.missing_assert_message = "warn"
clippy.missing_fields_in_debug = "warn"
clippy.same_functions_in_if_condition = "warn"
clippy.semicolon_if_nothing_returned = "warn"
clippy.should_panic_without_expect = "warn"
clippy.todo = "warn"
clippy.unseparated_literal_suffix = "warn"
clippy.use_self = "warn"

clippy.cargo_common_metadata = "warn"
clippy.negative_feature_names = "warn"
clippy.redundant_feature_names = "warn"
clippy.wildcard_dependencies = "warn"
# END LINEBENDER LINT SET

# Prefer using `#[expect(...)]` instead of `#[allow(...)]`.
# `#[expect(...)]` warns when the suppressed lint no longer fires.
clippy.allow_attributes = "warn"

[workspace.dependencies]
# `libm` routes every glam math function through the pure-Rust `libm` crate and
# `scalar-math` disables SIMD code paths, so results are bit-identical across
# platforms. Determinism is a sylva contract, not a performance afterthought.
# Exedra is pinned to a reviewed `main` revision until it publishes releases.
exedra_mesh = { default-features = false, features = ["libm"], git = "https://github.com/forest-rs/exedra", rev = "e286bcbebdfd86e2bc0e385c9557faede85a48f3" }
glam = { default-features = false, features = ["libm", "scalar-math"], version = "0.33.7" }
libm = "0.2.16"
serde = { default-features = false, features = ["alloc", "derive"], version = "1.0.228" }
sylva_grow = { path = "crates/sylva_grow", version = "0.0.1" }
sylva_mesh = { path = "crates/sylva_mesh", version = "0.0.1" }
sylva_skeleton = { path = "crates/sylva_skeleton", version = "0.0.1" }
sylva_species = { path = "crates/sylva_species", version = "0.0.1" }
