[package]
name = "sylva_species"
version = "0.0.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Species descriptions for sylva: what a kind of tree is, as data"
keywords = ["procedural", "tree", "vegetation", "no_std"]
categories = ["graphics", "game-development", "no-std"]
publish = false

[features]
serde = ["dep:serde", "sylva_grow/serde"]

[dependencies]
serde = { optional = true, workspace = true }
sylva_grow.workspace = true

[lints]
workspace = true
