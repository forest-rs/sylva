[package]
name = "sylva_grow"
version = "0.0.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Hierarchical rule-based tree growth producing the sylva skeleton IR"
keywords = ["procedural", "tree", "vegetation", "no_std"]
categories = ["graphics", "game-development", "no-std"]
publish = false

[features]
serde = ["dep:serde"]

[dependencies]
libm.workspace = true
serde = { optional = true, workspace = true }
sylva_skeleton.workspace = true

[lints]
workspace = true
